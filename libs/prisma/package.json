{
    "name": "@gearlink/prisma",
    "version": "1.0.0",
    "private": true,
    "main": "./src/index.ts",
    "scripts": {
        "generate": "prisma generate",
        "migrate:dev": "prisma migrate dev",
        "migrate:deploy": "prisma migrate deploy",
        "migrate:reset": "prisma migrate reset",
        "studio": "prisma studio",
        "seed": "ts-node src/seed.ts"
    },
    "dependencies": {
        "@prisma/client": "^5.7.1"
    },
    "devDependencies": {
        "prisma": "^5.7.1",
        "ts-node": "^10.9.2",
        "typescript": "^5.3.3"
    },
    "prisma": {
        "seed": "ts-node src/seed.ts"
    }
}