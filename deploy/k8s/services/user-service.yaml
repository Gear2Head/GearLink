apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: gearlink
  labels:
    app: user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: ghcr.io/kadir/gearlink/user-service:latest
        ports:
        - containerPort: 3002
        envFrom:
        - configMapRef:
            name: global-config
        - secretRef:
            name: global-secrets
        env:
        - name: PORT
          value: "3002"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: gearlink
spec:
  selector:
    app: user-service
  ports:
  - port: 80
    targetPort: 3002
  type: ClusterIP
