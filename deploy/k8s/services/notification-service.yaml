apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: gearlink
  labels:
    app: notification-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: ghcr.io/kadir/gearlink/notification-service:latest
        ports:
        - containerPort: 3005
        envFrom:
        - configMapRef:
            name: global-config
        - secretRef:
            name: global-secrets
        env:
        - name: PORT
          value: "3005"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: gearlink
spec:
  selector:
    app: notification-service
  ports:
  - port: 80
    targetPort: 3005
  type: ClusterIP
